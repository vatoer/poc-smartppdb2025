import { ThemeToggle } from "./theme-toggle.js";

export const ContainerThemeToggle = () => {
  return (
    <div className="fixed bottom-4 right-4">
      <ThemeToggle />
    </div>
  );
};

export default ContainerThemeToggle;
